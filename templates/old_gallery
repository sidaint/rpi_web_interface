# motioneye_clone/templates/gallery.html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gallery - {{ type|capitalize }}s</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-light">
<div class="container mt-4">
  <h2 class="mb-4">📁 {{ type|capitalize }} Gallery</h2>
  <a href="/" class="btn btn-secondary mb-4">← Back to Camera</a>
  <div class="row">
    {% for file in files %}
      <div class="col-md-4 mb-4">
        <div class="card bg-secondary text-white">
          {% if type == 'photo' %}
            <img src="/media/photos/{{ file }}" class="card-img-top" alt="Photo">
          {% elif type == 'video' %}
            <video class="w-100" controls>
              <source src="/media/{{ 'photos' if '../photos/' in file else 'videos' }}/{{ file|replace('../photos/', '') }}" type="video/mp4">
            </video>
          {% endif %}
          <div class="card-body">
            <p class="card-text small">{{ file }}</p>
            <a class="btn btn-sm btn-light mt-2" download href="/media/{{ 'photos' if type == 'photo' else 'videos' }}/{{ file|replace('../photos/', '') }}">⬇ Download</a>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</div>
</body>
</html>

